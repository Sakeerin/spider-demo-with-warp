"use client";
export default function PlaceholderPage() {
  return (
    <div className="max-w-2xl">
      <h2 className="mb-4 text-xl font-semibold">Admin Content (Temporary)</h2>
      <p className="text-sm text-gray-600">Temporary placeholder. The full form will be restored after we resolve the build issue.</p>
    </div>
  );
}
